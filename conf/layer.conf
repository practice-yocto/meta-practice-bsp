# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-practice-bsp"
BBFILE_PATTERN_meta-practice-bsp = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-practice-bsp = "6"

LAYERDEPENDS_meta-practice-bsp = "core"
LAYERSERIES_COMPAT_meta-practice-bsp = "kirkstone"
